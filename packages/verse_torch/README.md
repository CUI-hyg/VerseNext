# VerseTorch

Pure-Python/NumPy tensor & autograd engine (CPU-first).

Part of the [Verse](../../README.md) framework. Provides a PyTorch-like `Tensor`
class with dynamic computational graph and reverse-mode autograd, implemented
on top of NumPy with no PyTorch runtime dependency.
